# SiteWatcher the site watcher

SiteWatcher is a Scrapy application that watches sites for
changes and alerts the user when there is a new change.

## TODO
- move email logic to use scrapy instead of smtp
- add more sites
